const series = require ("./series.js")  // modulo propio
const fs = require ("fs")     // modulos nativos
 const express = require ("express") // modulo de terceros                          //modulos de terceros

console.log(express);